---
title: "What Is MoneyDev? Brazil's Unified Payments Gateway"
sidebarTitle: "Introduction"
description: "MoneyDev is a unified REST API for accepting PIX, boleto, and credit card payments in Brazil. Learn about its key features and the businesses it serves."
---

MoneyDev is a Brazilian payments gateway that lets businesses of all sizes accept the payment methods Brazilians actually use — PIX, boleto bancário, and credit and debit cards — through a single, developer-friendly REST API. Instead of integrating with multiple acquirers and banks, you connect once to MoneyDev and get access to Brazil's entire payment ecosystem, complete with real-time webhooks, a full-featured dashboard, and transparent, flat-rate pricing.

## Key Features

MoneyDev covers every major payment method in Brazil and the tooling you need to build a production-grade checkout experience.

<CardGroup cols={2}>
  <Card title="PIX Instant Payments" icon="bolt">
    Generate dynamic QR codes and payment links for PIX charges that settle in seconds, 24/7, including weekends and holidays.
  </Card>
  <Card title="Boleto Bancário" icon="file-invoice">
    Issue boletos with configurable due dates and automatic expiration handling. Ideal for customers who prefer cash-based payment.
  </Card>
  <Card title="Credit & Debit Cards" icon="credit-card">
    Accept Visa, Mastercard, Elo, Hipercard, and American Express with support for installments (parcelamento) of up to 12×.
  </Card>
  <Card title="Split Payments" icon="arrows-split-up-and-left">
    Automatically split a single transaction across multiple recipients — perfect for marketplace and platform business models.
  </Card>
  <Card title="Recurring Billing" icon="arrow-rotate-right">
    Create subscriptions and recurring charges with flexible billing cycles, automatic retries, and dunning management built in.
  </Card>
  <Card title="Webhooks" icon="webhook">
    Receive real-time event notifications for payment status changes, refunds, disputes, and more, delivered reliably to your endpoint.
  </Card>
</CardGroup>

## How the API Works

MoneyDev exposes a standard **REST API** that communicates exclusively over HTTPS. All request and response bodies are formatted as **JSON**. Authentication is handled via API keys passed in the `Authorization` header — no OAuth flow required.

**Base URL**

```text
https://api.moneydev.com.br/v1
```

Every resource in the API — payments, customers, refunds, subscriptions — lives under this base URL and follows predictable, resource-oriented URL conventions. Standard HTTP verbs apply:

| Verb     | Use                                      |
|----------|------------------------------------------|
| `GET`    | Retrieve a resource or list resources    |
| `POST`   | Create a new resource                    |
| `PATCH`  | Update an existing resource              |
| `DELETE` | Cancel or remove a resource              |

HTTP status codes reflect the outcome of every request: `2xx` for success, `4xx` for client errors (bad request, authentication failure, validation), and `5xx` for server-side issues. Error responses always include a machine-readable `code` field and a human-readable `message` to make debugging straightforward.

## Who MoneyDev Is For

MoneyDev is designed to serve any business that sells to customers in Brazil.

**E-commerce stores** use MoneyDev to offer the complete set of Brazilian payment methods at checkout — reducing cart abandonment by letting customers pay the way they prefer, whether that's scanning a PIX QR code, printing a boleto, or paying in installments on a credit card.

**SaaS companies** rely on MoneyDev's recurring billing engine to manage subscriptions, collect monthly or annual plan fees in BRL, and handle failed payments automatically with smart retry logic.

**Marketplaces and platforms** build on MoneyDev's split payment infrastructure to collect a transaction from a buyer and instantly route funds to the correct seller, minus the platform's commission — without writing custom ledger code.

If your business collects money from people in Brazil, MoneyDev gives you the infrastructure to do it reliably, securely, and at scale.
